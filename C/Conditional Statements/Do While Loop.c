#include <stdio.h>
#include <stdlib.h>

int main() {
    int num1 = 1;
    int num2 = 2;

    do {
        printf("Hello World!");
    } while (num1 > num2); // First it runs the code then it checks the condition
}