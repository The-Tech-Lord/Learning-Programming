#include <stdio.h>
#include <stdlib.h>

struct Node {
    int data;
    struct Node* next;
};

void delete() {
    struct Node* temp = (struct Node*)malloc(sizeof(struct Node));
    
}

int main() {

}