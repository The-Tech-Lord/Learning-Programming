let str = "Hello";
let str2 = 'Single quotes are ok too';
let phrase = `can embed another ${str}`; // Backticks are extended functionality quotes which allow you to embed
										 // variables and expressions into a string by wrapping them in ${}
